#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if [[ -f pyproject.toml && ! -x .venv/bin/python ]]; then
  echo "[resume] Missing .venv; run .agents/setup" >&2
  exit 1
fi

echo "[resume] Ready"
